/**
 * @deprecated Import from "../simulation/prepareSimulation" instead. Remove
 * after downstream consumers migrate to the canonical API and the compatibility
 * window closes.
 */
export * from "../simulation/prepareSimulation";
