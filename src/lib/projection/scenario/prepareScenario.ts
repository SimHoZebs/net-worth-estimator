/**
 * @deprecated Import from "../model/applyModelOverrides" instead. Remove after
 * downstream consumers migrate to the canonical API and the compatibility
 * window closes.
 */
export * from "../model/applyModelOverrides";
