/**
 * @deprecated Import createBaseDocument from "./documents". Remove after
 * downstream consumers migrate to the canonical API and the compatibility
 * window closes.
 */
export {
	createBaseDocument,
	createBaseDocument as createBasePack,
} from "./documents";
